#include <stdio.h>
int main ()
{
    // This program will ask the user to enter a value then print it
    int val;
    printf("Enter A Numeric Value: ");
    scanf("%d",&val);
    printf("You have entered: %d",val);
    return 0;
}
